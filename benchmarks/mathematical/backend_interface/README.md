# Native Backend Compatibility Benchmark

This Issue #040 benchmark verifies that dispatch through the backend registry
preserves the native FORM result and sampling contract. It introduces no
optional runtime dependency.

Run from an installed development checkout:

``` powershell
python benchmarks/mathematical/backend_interface/run.py
```

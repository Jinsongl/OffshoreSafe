# Floating-platform Reliability Benchmark

This Issue #072 benchmark applies a screening response surface to normalized
platform pitch using uncertain Hs, Tp, current speed, and mooring stiffness. It
checks the reference-state response and fixed native UQRA FORM result.

Run with `python benchmarks/offshore/floating_reliability/run.py`.

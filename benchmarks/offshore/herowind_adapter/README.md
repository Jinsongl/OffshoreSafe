# HEROWIND Adapter Benchmark

This Issue #053 fixture follows the verified HEROWIND comma-header and
whitespace-data result format. It validates unified channels, units, hashes,
and a known tower-base moment without executing HEROWIND.
